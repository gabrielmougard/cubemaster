//! Port of `xyflow-react/src/contexts/`.
//!
//! Status: Phase 0 — stubs. The TS folder only contains two files:
//!   * `NodeIdContext.ts` → [`node_id`]
//!   * `StoreContext.ts`  → [`store_ctx`] (which re-exports [`crate::context`])

pub mod node_id;
pub mod store_ctx;
